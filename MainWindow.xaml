<Window x:Class="Hercules.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Hercules.Views"
        xmlns:trait="clr-namespace:Hercules.Traits"
        Title="MainWindow" Height="450" Width="800">

    <Grid Background="#252525">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <views:ToolbarView Grid.Row="0" Panel.ZIndex="1"/>

        <Canvas Grid.Row="1">

            <ItemsControl ItemsSource="{Binding MainWindow_connections}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Canvas>

                            <Line X1="{Binding sourceStartPosX}" 
                                  Y1="{Binding sourceStartPosY}" 
                                  X2="{Binding targetStartPosX}" 
                                  Y2="{Binding targetStartPosY}"
                                  Stroke="#0078D4" 
                                  StrokeThickness="1"/>

                            <Polygon Points="-10,-5 0,0 -10,5" 
                                     Fill="#0078D4" 
                                     Stroke="#0078D4" 
                                     StrokeThickness="1">
                                <Polygon.RenderTransform>
                                    <TransformGroup>
                                        <RotateTransform Angle="{Binding arrowAngle}" />
                                        <TranslateTransform X="{Binding targetStartPosX}" Y="{Binding targetStartPosY}" />
                                    </TransformGroup>
                                </Polygon.RenderTransform>
                            </Polygon>

                        </Canvas>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>



            </ItemsControl>

            <ItemsControl ItemsSource="{Binding MainWindow_nodes}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding x}" />
                        <Setter Property="Canvas.Top" Value="{Binding y}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <views:NodeControl trait:DragTrait.IsDraggable="True" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </Canvas>

    </Grid>
</Window>