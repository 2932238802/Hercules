<UserControl x:Class="Hercules.Views.ToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="50" HorizontalAlignment="Stretch">

    <Border Background="#333333" BorderBrush="#0078D4" BorderThickness="0,0,0,2">
        <StackPanel Orientation="Horizontal" Margin="10,0">
            <Button Content="➕ 新建节点" Width="80" Margin="0,0,10,0" Padding="5"/>
            <Button Content="🗑️ 清空连线" Width="80" Margin="0,0,10,0" Padding="5"/>
            <Button Content="💾 保存" Width="60" Margin="0,0,10,0" Padding="5"/>
        </StackPanel>
    </Border>

</UserControl>